import React from 'react';
    
const Button = ({name}) => {
    
    return (
        <>
            <p className='cursor-pointer text-lg font-semibold'>{name}</p>
        </>
    )
    
}
    
export default Button;